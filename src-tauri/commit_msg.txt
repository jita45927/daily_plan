fix: 收起时基于黄线位置反推主窗口位置，展开后精确对齐屏幕边缘

collapse_window 中新增位置反推逻辑：
- 从屏幕 work_area 绝对坐标反推主窗口外框位置
- 左贴边：outer_x = wa.position.x - shadow_offset_x
- 右贴边：outer_x = wa_right - inner_width - shadow_offset_x
- 上贴边：outer_y = wa.position.y - shadow_offset_y
- 反推位置同步写入 config 和 collapsed_position

set_collapsed_state 展开时增加验证补偿：
- 读取 set_position 后的实际 inner_position
- 偏差 > 1px 则补偿调整 outer 位置
- 确保窗口精确恢复到与黄线对齐的位置
